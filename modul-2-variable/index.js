/** var, key untuk mengakses value baik di dalam scope maupun di luar scope */

// if (true) {
//   var nama = "firya";

//   console.log("var:", nama);
// }
// console.log("var:", nama);

/** let, key untuk mengakses value hanya di dalam scope-nya dan value bisa diubah */

if (true) {
  let nama = "firya";
  nama = "faturahman";
  console.log("let:", nama);
}

/** const, key untuk mengakses value hanya di dalam scope-nya dan value tidak bisa diubah */

if (true) {
  const nama = "asep";
  // nama = "pesa";
  console.log("const:", nama);
}
